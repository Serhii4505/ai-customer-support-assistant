# n8n workflows

Planned sequence: Webhook → HTTP Request to Python /chat → branch on handoff_required → record/notify handoff → Respond to Webhook.

The importable workflow will be added once the Python endpoint has been tested. Actual notification credentials and recipient are not configured.
